[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "harness-evaluation"
version = "0.3.0"
description = "Lightweight orchestration and evaluation utilities for external CLI agents"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
harness-eval = "harness_evaluation.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
