[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "harness-evaluation"
version = "0.2.0"
description = "Lightweight ingestion and evaluation utilities for external agent harness results"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
harness-eval = "harness_evaluation.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
